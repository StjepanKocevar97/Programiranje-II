Napisati funkciju koja ce izracunati i vratiti faktorijel danog cijelog broja iz intervala [0, 12]. Na primjeru u funkciji
main() pokazati uporabu napisane funkcije.

#include <stdio.h>
int faktorijel(int n);
int main(void) {
int i, f;
f = faktorijel(i);
} return 0;
}
int faktorijel(int n)
{ if (n < 0)
for (i = 0; i <= 12; i++) {
printf(" %d! = %d\n" , i, f);
int f = 1, i;
for (i = 2; i <= n; i++) f *= i;
return f;
}
