/* 
Izračunati aritmetičku sredinu parnih prirodnih brojeva do 100.
*/

#include <stdio.h>
 
main()
{
    int i;
    float s,broj,AS;  
    s=0;
    broj=0;    
    for(i=1;i<=100;i++)    
    {
        if(i % 2 == 0)      
            {
                s=s+i;
                broj=broj+1;
            }
    }    
    AS=s/broj;
    printf("Aritmeticka sredina parnih od 1 do 100 iznosi: %.3f",as);
     
    return 0;
}

