/*
Pretvorite sve decimalne brojeve u binarne.
*/

#include<stdio.h>
#include<conio.h>

int main()
{
    int dec, bin[99], j = 0;
    printf("Unesite decimalni broj po zelji: ");
    scanf("%d", &dec);
    while (dec != 0)
    {
        bin[i] = dec % 2;
        j++;
        dec = dec / 2;
    }
    printf("\nVrijednost binarnog je = ");
    for (j = (j - 1); j >= 0; j--)
        printf("%d", bin[j]);
    _getch();
    return 0;
} 
