#include<stdio.h>

/*Omoguci korisniku unos realnog broja te provjeri je li dijeljiv s 2 ili nije dijeljiv s dva*/

int main() 
{
	float X;
	printf("Unesite broj");
	scanf("%f", &X);
	if (X % 2 == 0) {
		printf("%f dijeljiv s dva", X);
	}
	else 
  {
		printf("%f nije dijeljiv s dva", X);
	}
	return 0;
}

*/ 
U ovom zadatku unosimo dva realna broja. Pomoću funkcije scanf unosimo 2 realna broja, if petljom provjevaramo da li je dijeljiv s 2. 
If else petlja nam omogućuje ispis broja ako nije dijeljiv s dva.Pošto se radi o dijeljenju realnih brojeva koristi se "%2==0", odnosno
dijeljenje s ostatkom.
*/
