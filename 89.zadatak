#include <stdio.h>
/*
Isprintajte sve vrijednosti engleske abecede na ekran.
*/

int main()
{
    int j;

    for (j = 0; j <= 26; j++)
    {
        printf("Sve vrijednosti engleske abecede  %c = %d\n", j, j);
    }

    return 0;
}
/*
Ovaj zadatak nam pokazuje kako prvo printamo vrijednosti engleske abecede. Pomocu for petlje svih 26 znakova
stavimo da prolaze kroz imaginarno polje. Preko %c i %d izjednacimo da bismo dobili numericke vrijednosti svih 26 znaka i obratno.
*/
