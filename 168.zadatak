Napisati funkciju koja ´ce vratiti duljinu predanog joj stringa. Na primjeru u funkciji main() pokazati uporabu napisane funkcije

#include <stdio.h>
int brznak(const char s[]);
int main(void)
{
char str [100] = "Koliko li ima ovdje znakova?";
printf("Ima %d znakova.", brznak(str ));
return 0;
}
int brznak(const char s[])
{
int i, br = 0;
for (i = 0; s[i] != ’\0’; i++)
br ++;
return br; //br == i
}
